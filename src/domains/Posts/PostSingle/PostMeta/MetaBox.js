import styled from 'styled-components';

const MetaBox = styled.p`
  font-size: 18px;
  line-height: 1.58;
  font-weight: 400;
  color: rgba(0, 0, 0, .64);
  margin-bottom: 15px;
  text-align: center;
`;

export default MetaBox;
